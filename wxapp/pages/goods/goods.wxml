<!--index.wxml-->
<view class="container">
  <goods-card wx:key="*this" wx:for="{{items}}" wx:for-item="item" data="{{item}}" bind:onCall="onCall"></goods-card>
  <loading loadingView="{{loadingView}}"></loading>
</view>